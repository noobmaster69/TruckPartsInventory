# Copy this file to .env and adjust as needed.

# SQLite database file. Path is relative to the prisma/ schema directory,
# so this resolves to <project-root>/data/inventory.db
DATABASE_URL="file:../data/inventory.db"

# Express API port (Vite proxies /api -> this port during dev)
PORT=3001
